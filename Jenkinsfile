pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "first echo"
            }
        }
        stage('Test') {
            steps {
                echo "second echo"
            }
        }
    }
}
